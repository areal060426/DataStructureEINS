package Activity3;

public class Rules {
/*
No Arrays.sort().
Must use Scanner.
Must use at least one array.
Group 4 must use a 2D array.
Students must manually implement sorting.
Program must not crash when invalid menu options are entered.
Sorting must not destroy related information.
Students must explain their sorting algorithm during presentation.
Each member must explain at least one portion of the code.
Instructor may modify one input during demonstration to verify that the program is not hard-coded.
*/
}
